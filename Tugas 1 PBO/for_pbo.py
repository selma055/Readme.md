names = ["Selma", "Selma", "Selma", "Selma"]

for name in names:
    if len(name) % 2 == 0:
        print(name + " fakultas teknik sistem informasi")
    else:
        print(name + " fakultas teknik sistem informasi")